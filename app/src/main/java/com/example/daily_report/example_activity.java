package com.example.daily_report;

import androidx.appcompat.app.AppCompatActivity;

import android.content.Intent;
import android.os.Bundle;
import android.widget.TextView;

public class example_activity extends AppCompatActivity {

    /*

    private TextView changedView;



    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_examplelayout);

        changedView=findViewById(R.id.changedView);

        Intent i=getIntent();
        String str=i.getStringExtra("nextActivityUse");
        changedView.setText(str);

    }

     */
}